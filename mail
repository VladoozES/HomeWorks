import re


def check_correct(mail: str):
    if len(mail) < 4 or len(mail) > 31:
        print('False')
    else:
        print(not re.match('^(\w|\d|\-|\.)*([0-9]|[a-z])$', mail.lower())  is None)


while True:
    v_mail = input('Введите почту: ')
    check_correct(v_mail)
